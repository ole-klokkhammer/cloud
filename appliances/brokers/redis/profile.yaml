name: redis
description: Redis container profile
config:
  user.host: "core"
  limits.cpu: "2"
  limits.memory: 32GB
  security.nesting: "true"
  security.privileged: "false"
devices:
  eth0:
    name: eth0
    nictype: bridged
    parent: br0
    type: nic
    hwaddr: 00:16:3e:03:e1:2b
  root:
    path: /
    pool: zpool
    type: disk
project: default


