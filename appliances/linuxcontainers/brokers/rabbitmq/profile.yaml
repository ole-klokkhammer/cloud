name: rabbitmq
description: RabbitMQ container profile
config:
  user.host: "core"
  limits.cpu: "2"
  limits.memory: 32GB
  linux.kernel_modules: ip_tables, ip6_tables, netlink_diag, nf_nat, overlay
  raw.lxc: |
    lxc.apparmor.profile=unconfined
    lxc.cap.drop=
  security.nesting: "false"
  security.privileged: "false"
devices:
  eth0:
    name: eth0
    nictype: bridged
    parent: br0 
    type: nic
    hwaddr: 00:16:3e:8c:ff:60
  root:
    path: /
    pool: zpool
    type: disk
  rabbitmq-data:
    type: disk
    path: /var/lib/rabbitmq
    source: /ssd/appdata/rabbitmq/data
    shift: "true"
  enabled-plugins:
    type: disk
    path: /etc/rabbitmq/enabled_plugins
    source: /ssd/appdata/rabbitmq/config/enabled_plugins
    shift: "true"
  rabbitmq-config:
    type: disk
    path: /etc/rabbitmq/rabbitmq.conf
    source: /ssd/appdata/rabbitmq/config/rabbitmq.conf
    shift: "true"
project: default