services:
  neo-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - MQTT_SERVER=ws://192.168.10.207:8000/mqtt