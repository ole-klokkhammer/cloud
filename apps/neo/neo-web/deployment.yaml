apiVersion: apps/v1
kind: Deployment
metadata:
  name: neo-web
  namespace: neo
  labels:
    app: neo-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: neo-web
  template:
    metadata:
      labels:
        app: neo-web
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - master0
      containers:
        - name: neo-web
          image: olklokk/neo-web:0.9.20
          ports:
            - containerPort: 3000
          env: 
            - name: AUTH_TRUST_HOST
              value: "true"
            - name: AUTH_URL
              valueFrom:
                secretKeyRef:
                  name: neo-config
                  key: AUTH_URL
            - name: AUTH_SECRET
              valueFrom:
                secretKeyRef:
                  name: neo-config
                  key: AUTH_SECRET
            - name: KEYCLOAK_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: neo-config
                  key: KEYCLOAK_CLIENT_ID
            - name: KEYCLOAK_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: neo-config
                  key: KEYCLOAK_CLIENT_SECRET
            - name: KEYCLOAK_ISSUER
              valueFrom:
                secretKeyRef:
                  name: neo-config
                  key: KEYCLOAK_ISSUER
            - name: KEYCLOAK_MOBILE_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: neo-config
                  key: KEYCLOAK_MOBILE_CLIENT_ID
            - name: KEYCLOAK_MOBILE_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: neo-config
                  key: KEYCLOAK_MOBILE_CLIENT_SECRET
            - name: KEYCLOAK_MOBILE_AUTHORIZE_ENDPOINT
              valueFrom:
                secretKeyRef:
                  name: neo-config
                  key: KEYCLOAK_MOBILE_AUTHORIZE_ENDPOINT
            - name: KEYCLOAK_MOBILE_TOKEN_ENDPOINT
              valueFrom:
                secretKeyRef:
                  name: neo-config
                  key: KEYCLOAK_MOBILE_TOKEN_ENDPOINT