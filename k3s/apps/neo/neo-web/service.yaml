apiVersion: v1
kind: Service
metadata:
  name: neo-web
  namespace: neo
spec:
  type: ClusterIP
  selector:
    app: neo-web
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: neo-web-external
  namespace: neo
spec:
  type: LoadBalancer
  selector:
    app: neo-web
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP