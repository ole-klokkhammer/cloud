apiVersion: v1
kind: Service
metadata:
  name: trilium
  namespace: apps
  labels:
    app: trilium
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: trilium