apiVersion: apps/v1
kind: Deployment
metadata:
  name: bluetooth2amqp
  namespace: bridges
  labels:
    app: bluetooth2amqp
spec:
  revisionHistoryLimit: 0
  selector:
    matchLabels: 
      app: bluetooth2amqp
  template:
    metadata:
      labels:
        app: bluetooth2amqp
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - master0
      containers:
        - name: bluetooth2amqp
          image: ghcr.io/ole-klokkhammer/bluetooth2amqp:1.0.64
          securityContext:
            capabilities:
              add:
                - NET_ADMIN
                - NET_RAW
            appArmorProfile:
              type: Unconfined
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - bluetoothctl list | grep -q 'Controller' && exit 0 || exit 1
            initialDelaySeconds: 10
            periodSeconds: 30
          env:
            - name: LOG_LEVEL
              value: "info"
            - name: SCHEDULES_FILE
              value: "/app/schedules.yaml" 
          envFrom:
          - secretRef:
              name: bluetooth2amqp-secrets
          volumeMounts:
            - mountPath: /var/run/dbus
              name: dbus-socket
            - mountPath: /app/schedules.yaml
              name: schedules
              subPath: schedules.yaml
              readOnly: true
      volumes:
        - name: dbus-socket
          hostPath:
            path: /var/run/dbus
            type: Directory
        - name: schedules
          configMap:
            name: bluetooth2amqp-schedules
