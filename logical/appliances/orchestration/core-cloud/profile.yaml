name: core-cloud
description: Cloud profile
config:
  limits.cpu: "8"
  limits.memory: 32GB
devices:
  eth0:
    name: eth0
    nictype: bridged
    parent: br0
    type: nic
  root:
    path: /
    pool: zpool
    type: disk
project: default