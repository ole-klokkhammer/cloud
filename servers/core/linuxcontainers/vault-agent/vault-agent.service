[Unit]
Description=Vault Agent for MyApp
After=network.target

[Service]
EnvironmentFile=-/etc/vault.d/vault.env
ExecStart=vault agent -config=/etc/vault.d/agent.hcl
Restart=always

[Install]
WantedBy=multi-user.target
